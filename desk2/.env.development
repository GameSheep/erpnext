# Dev-mode overrides. The Vite dev server reads these.
# VITE_BASE_NAME is intentionally empty in dev so React Router serves at "/".
VITE_BASE_NAME=
# Set these to point at a local Frappe bench.
# VITE_SOCKET_PORT=9000
# VITE_SITE_NAME=localhost
